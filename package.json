{
  "name": "factory-management-platform",
  "version": "1.0.0",
  "description": "A comprehensive Factory Management Platform for manufacturing operations",
  "main": "backend/dist/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm run build && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["factory", "management", "manufacturing", "react", "nodejs", "mongodb"],
  "author": "Factory Management Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
